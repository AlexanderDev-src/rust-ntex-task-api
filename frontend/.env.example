# Leave unset to use the /api prefix (Vite dev proxy, or the nginx proxy in Docker).
# Set it only when the API lives on another origin.
# TODO: copy this file to .env.local if you need an absolute URL.
# VITE_API_URL=http://127.0.0.1:8080
